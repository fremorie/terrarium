export const TRANSITION_HALF_DURATION = 1

// Resting rotation of the whole scene around Y.
// At this exact angle the environment map lines up
// so that it looks like the sun casts the baked shadows.
export const BASE_SCENE_ROTATION_Y = -2.8
